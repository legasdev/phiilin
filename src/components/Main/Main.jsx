import React from 'react';

import s from './main.module.css';

const Main = props => {
    return (
        <main className={s.main}>
            Главная
        </main>
    );
}

export default Main;