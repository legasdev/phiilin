const express=require("express"),app=express();app.get("/",(request,response)=>{throw new Error("oops")}),app.get("/auth/me",(req,res)=>{res.send({user:"dsa",statusCode:0})}),app.use((err,request,response,next)=>{console.log(err),response.status(500).send("Something broke!")}),app.listen(8080,()=>{console.log("Ok. Server working...")});